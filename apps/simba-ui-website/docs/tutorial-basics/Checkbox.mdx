---
sidebar_position: 2
---

import { MyCheckbox } from "tailwind-simba-ui";
import styles from "./pages.module.css";

# Checkbox

Checkbox component is used in forms when a user needs to select multiple values from several options.

### Import

```js
import { MyCheckbox } from "tailwind-simba-ui";
```

### Usage

<!-- insert button example here -->

```js
<MyCheckbox><MyCheckbox>
```

### Checkbox Sizes

export const CheckboxSizes = ({ size }) => (
  <div className="flex h-24 items-center border-2	rounded-xl mb-4">
    <div className="pr-2 pl-2">
      <MyCheckbox size="xs"></MyCheckbox>
    </div>
    <div className="pr-2">
      <MyCheckbox size="md"></MyCheckbox>
    </div>
    <div>
      <MyCheckbox></MyCheckbox>
    </div>
  </div>
);

Use the size prop to change the size of the button. You can set the value to **xs**, **md**, or **lg**.

<CheckboxSizes></CheckboxSizes>

```js
<div>
  <MyCheckbox size="xs"></MyCheckbox>
  <MyCheckbox size="sm"></MyCheckbox>
  <MyCheckbox size="md"></MyCheckbox>
  <MyCheckbox size="lg"></MyCheckbox>
</div>
```

### Checkbox with custom color

You can override the color scheme of the Checkbimport styles from "./pages.module.css";
ox to green....and (add more colors to the package)

```js
<div>
  <MyButton color="green"></MyButton>
</div>
```
