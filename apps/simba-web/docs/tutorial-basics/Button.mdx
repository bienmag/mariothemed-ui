---
sidebar_position: 1
---

import { Button } from "tailwind-simba-ui";
import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import { CiMail } from "react-icons/ci";

Button component is used to trigger an action or event, such as submitting a form, opening a Dialog, canceling an action, or performing a delete operation.

## **Import**

```js
import { Button } from "tailwind-simba-ui";
```

## **Usage**

export const BasicUsage = () => (
  <div className="border-2 rounded-xl mb-4">
    <div className="m-2">
      <Button color="yellow">Button</Button>
    </div>
  </div>
);

<BasicUsage></BasicUsage>

```js
<Button color="yellow">Button</Button>
```

## **Button Sizes**

export const ButtonSizes = ({ size }) => (
  <div className="flex h-24 items-center border-2	rounded-xl mb-4">
    <div className="pr-2 pl-2">
      <Button color="yellow" size="xs">
        Button
      </Button>
    </div>
    <div className="pr-2">
      <Button color="yellow" size="sm">
        Button
      </Button>
    </div>
    <div className="pr-2">
      <Button color="yellow" size="md">
        Button
      </Button>
    </div>
    <div>
      <Button color="yellow" size="lg">
        Button
      </Button>
    </div>
  </div>
);

Use the size prop to change the size of the button. You can set the value to **xs**, **sm**, **md**, or **lg**.

<ButtonSizes></ButtonSizes>{" "}

```js
<div>
  <Button color="yellow" size="xs">
    Button
  </Button>
  <Button color="yellow" size="sm">
    Button
  </Button>
  <Button color="yellow" size="md">
    Button
  </Button>
  <Button color="yellow" size="lg">
    Button
  </Button>
</div>
```

## **Button variants**

export const ButtonVariants = ({ variant }) => (
  <div className="flex h-24 items-center border-2	rounded-xl mb-4">
    <div className="pr-2 pl-2">
      <Button color="yellow" variant="solid">
        Button
      </Button>
    </div>
    <div className="pr-2">
      <Button color="yellow" variant="outline">
        Button
      </Button>
    </div>
    <div className="pr-2">
      <Button color="yellow" variant="ghost">
        Button
      </Button>
    </div>
    <div>
      <Button color="yellow" variant="link">
        Button
      </Button>
    </div>
  </div>
);

Use the variant prop to change the visual style of the Button. You can set the value to **solid**, **ghost**, **outline**, or **link**.

<ButtonVariants></ButtonVariants>

```js
<div>
  <Button color="yellow" variant="solid">
    Button
  </Button>
  <Button color="yellow" variant="outline">
    Button
  </Button>
  <Button color="yellow" variant="ghost">
    Button
  </Button>
  <Button color="yellow" variant="link">
    Button
  </Button>
</div>
```

## **Button color**

export const ButtonColors = ({ color }) => (
  <div className="flex h-24 items-center border-2	rounded-xl mb-4">
    <div className="pr-2 pl-2">
      <Button color="yellow">Button</Button>
    </div>
    <div className="pr-2">
      <Button color="red">Button</Button>
    </div>
    <div className="pr-2">
      <Button color="blue">Button</Button>
    </div>
    <div>
      <Button color="green">Button</Button>
    </div>
  </div>
);

Use the color prop to change the visual style of the Button. You can set the value to **yellow**, **red**, **blue**, or **green**.

<ButtonColors></ButtonColors>

```js
<div>
  <Button color="yellow">Button</Button>
  <Button color="red">Button</Button>
  <Button color="blue">Button</Button>
  <Button color="green">Button</Button>
</div>
```

## **Button with icon**

You can add an icon to your button. Use the **leftIcon** prop to place the image on the left of the button and **rightIcon** to place the image on the right of the button.
:::info
Please note that the icon should be a react element and not a string!
:::

export const ButtonWithIcon = ({ leftIcon, rightIcon }) => (
  <div className="flex h-24 items-center border-2	rounded-xl mb-4">
    <div className="pr-2 pl-2">
      <Button color="yellow" leftIcon={<CiMail />}>
        Button
      </Button>
    </div>
    <div className="pr-2">
      <Button color="red" rightIcon={<CiMail />}>
        Button
      </Button>
    </div>
  </div>
);

<ButtonWithIcon></ButtonWithIcon>

```js
<div>
  <Button color="yellow" leftIcon={<Icon />}>
    Button
  </Button>
  <Button color="red" rightIcon={<Icon />}>
    Button
  </Button>
</div>
```

## **Customize your button**

:::danger
CHECK THIS!
:::

You can customize your button with className
OR
override the styles provided with the ui library

```
<Button size="xs" height="120px"/> // will override the height set in "xs"
```

export const CustomButton = ({ leftIcon, rightIcon }) => (
  <div className="flex h-24 items-center border-2	rounded-xl mb-4">
    <div className="pr-2 pl-2">
      <Button
        className="w-40 h-20 flex justify-center items-center"
        size="xs"
        color="yellow"
      >
        Click me
      </Button>{" "}
    </div>
    <div className="pr-2">
      <Button color="red">Button</Button>
    </div>
  </div>
);

<CustomButton></CustomButton>

```
<Button className="w-40 h-20 flex justify-center items-center" size="xs" color="yellow"/>
```
